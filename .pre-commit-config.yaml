repos:
  - repo: local
    hooks:
      - id: verify-pre-commit
        name: verify-pre-commit
        entry: python3 scripts/verify.py --mode pre-commit --changed-files
        language: system
        pass_filenames: true
